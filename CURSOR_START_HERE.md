# Cursor start here

Первое сообщение агенту:

```text
Открой specs/labmind-core/cursor_master_prompt.md и следуй ему. Начни с Milestone 0–1 из tasks.md: проверь структуру репозитория, Supabase schema, RLS, private storage bucket и план реализации upload PDF → parser → normalized results. Не пиши код, пока не покажешь краткий execution plan и список файлов, которые будешь менять.
```

После ответа агента:

```text
Ок, реализуй только Milestone 0–1. Не трогай email ingest и crawler цен. После реализации дай список изменённых файлов, команды для запуска и security notes.
```
